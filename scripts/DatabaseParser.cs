using Godot;
using System;

public class DatabaseParser : Node
{
    /* 
    public File m_File = new File();
    public object m_Data;

    public void LoadData(string filepath)
    {
        if (filepath == null) return;
        if (m_File.FileExists(filepath)) return;

        m_File.Open(filepath, 1);
        m_Data = JSON.Parse(m_File.GetAsText()).Result;
        m_File.Close();
    }
    */
}